# Docker Compose 覆盖配置示例
# 复制此文件为 docker-compose.override.yml 并根据需要修改

version: '3.8'

services:
  backend:
    # 开发模式：挂载源代码实现热重载
    # volumes:
    #   - ./backend:/app
    #   - /app/node_modules
    
    # 自定义环境变量
    # environment:
    #   - NODE_ENV=development
    #   - LOG_LEVEL=debug
    
    # 端口映射
    # ports:
    #   - "3001:3000"  # 使用不同的端口

  frontend:
    # 开发模式：挂载源代码
    # volumes:
    #   - ./frontend:/app
    #   - /app/node_modules
    
    # 自定义端口
    # ports:
    #   - "8080:80"

  # 添加数据库管理工具（可选）
  # adminer:
  #   image: adminer
  #   restart: always
  #   ports:
  #     - 8081:8080
  #   networks:
  #     - 2fa-network

  # 添加Redis缓存（可选）
  # redis:
  #   image: redis:alpine
  #   restart: always
  #   volumes:
  #     - redis-data:/data
  #   networks:
  #     - 2fa-network

# volumes:
#   redis-data:


